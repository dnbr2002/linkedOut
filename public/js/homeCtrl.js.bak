var homeControllers = angular.module('homeControllers', []);

homeControllers.controller('homeCtrl', function($scope, $http) {
    // Does things for the home controller to figure out on page load.

});
